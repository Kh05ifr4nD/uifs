import { UIMetrics } from "metrics.slint";
export component SplitLine inherits Rectangle {
    height: UIMetrics.SplitL.h;
    background: UIMetrics.SplitL.color;
    border-radius: UIMetrics.SplitL.r;
}
