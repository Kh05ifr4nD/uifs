import { UIMetrics } from "metrics.slint";
export component SplitLine inherits Rectangle {
    height: UIMetrics.SplitLine.height;
    background: UIMetrics.SplitLine.color;
    border-radius: UIMetrics.SplitLine.radius;
}
