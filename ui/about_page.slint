import { UIMetrics } from "./metrics.slint";
import { AboutSlint, VerticalBox } from "std-widgets.slint";
export component AboutPage inherits VerticalBox {
    alignment: UIMetrics.AboutP.a;
    width: UIMetrics.AboutP.w;
    AboutSlint { }
}
