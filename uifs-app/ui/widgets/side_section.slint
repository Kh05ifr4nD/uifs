import { GroupBox, StyleMetrics } from "std-widgets.slint";
export component SideSection inherits GroupBox {
    VerticalLayout {
        spacing: StyleMetrics.layout-spacing;

        @children
    }
}